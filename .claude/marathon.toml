# Project kind: this repository is context (skills, manifests, prose), not production
# source code.
[project]
kind = "context"

# Remote platform and the command that publishes a finished branch.
[remote]
platform = "github"
publish = "gh pr create"
